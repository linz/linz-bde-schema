sudo: false
language: C++
services:
  - postgresql: "9.4"
addons:
  apt:
    packages:
    - postgresql-9.4
    - postgresql-9.4-pgtap
    - postgresql-server-dev-9.4
script:
  - make
  - make check || { cat regression.diffs; false; }
